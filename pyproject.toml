[project]
name = "dwbs"
version = "0.1.0"
description = "Deterministic Work Breakdown System - Core Logic"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0.0",
    "typing_extensions",
]

[tool.pytest.ini_options]
pythonpath = [
  "src"
]
